# This file makes `backend/app/` a Python package.
# It can be empty — its presence is what matters.
# Without it, `from app.config import settings` would fail
# because Python wouldn't recognize `app` as a package.
